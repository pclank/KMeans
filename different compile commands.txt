
gprof km gmon.out > profile-data.txt

/ Serial
gcc -Wall -pg kmeans.c -o km -march=sandybridge -mavx2 -fopt-info-vec-optimized

// Parallel
gcc -Wall -pg kmeans_omp.c -o km_omp -O4 -fopenmp -march=sandybridge -mavx2 -fopt-info-vec-optimized